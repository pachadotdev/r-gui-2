name: Build macOS Installer

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version string (e.g. 1.2.3)'
        required: false
        default: '0.0.0-dev'

jobs:
  build-mac:
    name: Build and package for macOS
    runs-on: macos-13

    steps:
      # ── 1. Source code ────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Qt6 build environment ──────────────────────────────────────────
      # brew install qt6 brings in the full Qt6 suite including WebEngine and
      # WebChannel, which q requires.  R is pre-installed on macos-13 runners.
      - name: Set up Qt6 on macOS
        run: |
          brew install qt6
          echo "$(brew --prefix qt6)/bin" >> $GITHUB_PATH

      # ── 3. Configure & build ──────────────────────────────────────────────
      - name: Configure CMake
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt6)"

      - name: Build
        run: cmake --build build -j$(sysctl -n hw.logicalcpu)

      # ── 4. Create .app bundle ─────────────────────────────────────────────
      # The binary resolves gogh-themes and fonts as ./gogh-themes and ./fonts
      # relative to itself, so both directories must live inside Contents/MacOS.
      - name: Create .app bundle
        run: |
          mkdir -p RGUI2/Contents/MacOS
          mkdir -p RGUI2/Contents/Resources

          cp build/bin/rgui2 RGUI2/Contents/MacOS/
          cp -r build/bin/gogh-themes RGUI2/Contents/MacOS/gogh-themes
          cp -r build/bin/fonts       RGUI2/Contents/MacOS/fonts
          cp README.md                RGUI2/Contents/Resources/

          cat > RGUI2/Contents/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleExecutable</key>
            <string>q</string>
            <key>CFBundleIdentifier</key>
            <string>com.pacha.r-gui-2</string>
            <key>CFBundleName</key>
            <string>R GUI 2</string>
            <key>CFBundleVersion</key>
            <string>${{ inputs.version }}</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
          </dict>
          </plist>
          EOF

      # ── 5. Bundle Qt frameworks ───────────────────────────────────────────
      - name: Bundle Qt frameworks (macdeployqt)
        run: |
          "$(brew --prefix qt6)/bin/macdeployqt" RGUI2

      # ── 6. Create DMG ─────────────────────────────────────────────────────
      - name: Create DMG
        run: |
          mkdir -p dmg-root
          cp -R RGUI2 dmg-root/
          hdiutil create \
            -volname "R GUI 2" \
            -srcfolder dmg-root \
            -ov \
            -format UDZO \
            r-gui-2-${{ inputs.version }}-mac.dmg

      # ── 7. Upload artifact ────────────────────────────────────────────────
      - name: Upload macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: R-GUI-2-${{ inputs.version }}-macOS
          path: r-gui-2-*-mac.dmg
          if-no-files-found: error
